#!/usr/bin/env bash
set -euo pipefail
branch="$(git branch --show-current)"
if [[ "$branch" == "main" ]]; then
  printf 'Direct commits to main are disabled. Commit to develop or a topic branch and use a pull request.\n' >&2
  exit 1
fi
