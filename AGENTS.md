# Repository instructions

Read `docs/AI_AGENT.md` before changing code. Preserve the mute-ownership safety invariant, avoid shell execution in the application, and run `./scripts/test.sh`, `./scripts/build.sh`, and `git diff --check` before completing work. Do not commit or push directly to `main`.
